[package]
name = "litegraph-server-rust"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
axum = "0.7"
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tower-http = { version = "0.6", features = ["fs"] }
