# 文件文档：`src/lgraphcanvas/shared/scratch.js`

## 所属模块介绍

- 模块：`src/lgraphcanvas/shared`
- 介绍来源：[AUTO-MODULE] 自动生成
- 介绍：
> [AUTO-MODULE] 模块 `src/lgraphcanvas/shared` 的职责为：共享支撑层，提供默认配置与复用工具。
> 规模：包含 1 个文件，导出 7 项（AUTO 7 项），耦合强度 3。
> 关键耦合：出边 低耦合/未发现内部下游依赖；入边 `src/lgraphcanvas/modules`(3)。
> 主要导出：`link_bounding`、`margin_area`、`temp`、`temp_vec2`、`tempA`、`tempB`。
> 代表文件：`scratch.js`。

- 导出项数量：7
- AUTO 说明数量：7

## 1. `temp`

- 类型：`variable`
- 位置：`src/lgraphcanvas/shared/scratch.js:1-1` (`#L1`)
- 说明来源：[AUTO] 自动回退
- 说明：
> [AUTO] 导出变量 `temp`（const），定义于 `src/lgraphcanvas/shared/scratch.js`。

- 代码片段（L1-L6）：
```js
export const temp = new Float32Array(4);
export const temp_vec2 = new Float32Array(2);
export const tmp_area = new Float32Array(4);
export const margin_area = new Float32Array(4);
export const link_bounding = new Float32Array(4);
export const tempA = new Float32Array(2);
```

## 2. `temp_vec2`

- 类型：`variable`
- 位置：`src/lgraphcanvas/shared/scratch.js:2-2` (`#L2`)
- 说明来源：[AUTO] 自动回退
- 说明：
> [AUTO] 导出变量 `temp_vec2`（const），定义于 `src/lgraphcanvas/shared/scratch.js`。

- 代码片段（L2-L7）：
```js
export const temp_vec2 = new Float32Array(2);
export const tmp_area = new Float32Array(4);
export const margin_area = new Float32Array(4);
export const link_bounding = new Float32Array(4);
export const tempA = new Float32Array(2);
export const tempB = new Float32Array(2);
```

## 3. `tmp_area`

- 类型：`variable`
- 位置：`src/lgraphcanvas/shared/scratch.js:3-3` (`#L3`)
- 说明来源：[AUTO] 自动回退
- 说明：
> [AUTO] 导出变量 `tmp_area`（const），定义于 `src/lgraphcanvas/shared/scratch.js`。

- 代码片段（L3-L8）：
```js
export const tmp_area = new Float32Array(4);
export const margin_area = new Float32Array(4);
export const link_bounding = new Float32Array(4);
export const tempA = new Float32Array(2);
export const tempB = new Float32Array(2);

```

## 4. `margin_area`

- 类型：`variable`
- 位置：`src/lgraphcanvas/shared/scratch.js:4-4` (`#L4`)
- 说明来源：[AUTO] 自动回退
- 说明：
> [AUTO] 导出变量 `margin_area`（const），定义于 `src/lgraphcanvas/shared/scratch.js`。

- 代码片段（L3-L8）：
```js
export const tmp_area = new Float32Array(4);
export const margin_area = new Float32Array(4);
export const link_bounding = new Float32Array(4);
export const tempA = new Float32Array(2);
export const tempB = new Float32Array(2);

```

## 5. `link_bounding`

- 类型：`variable`
- 位置：`src/lgraphcanvas/shared/scratch.js:5-5` (`#L5`)
- 说明来源：[AUTO] 自动回退
- 说明：
> [AUTO] 导出变量 `link_bounding`（const），定义于 `src/lgraphcanvas/shared/scratch.js`。

- 代码片段（L3-L8）：
```js
export const tmp_area = new Float32Array(4);
export const margin_area = new Float32Array(4);
export const link_bounding = new Float32Array(4);
export const tempA = new Float32Array(2);
export const tempB = new Float32Array(2);

```

## 6. `tempA`

- 类型：`variable`
- 位置：`src/lgraphcanvas/shared/scratch.js:6-6` (`#L6`)
- 说明来源：[AUTO] 自动回退
- 说明：
> [AUTO] 导出变量 `tempA`（const），定义于 `src/lgraphcanvas/shared/scratch.js`。

- 代码片段（L3-L8）：
```js
export const tmp_area = new Float32Array(4);
export const margin_area = new Float32Array(4);
export const link_bounding = new Float32Array(4);
export const tempA = new Float32Array(2);
export const tempB = new Float32Array(2);

```

## 7. `tempB`

- 类型：`variable`
- 位置：`src/lgraphcanvas/shared/scratch.js:7-7` (`#L7`)
- 说明来源：[AUTO] 自动回退
- 说明：
> [AUTO] 导出变量 `tempB`（const），定义于 `src/lgraphcanvas/shared/scratch.js`。

- 代码片段（L3-L8）：
```js
export const tmp_area = new Float32Array(4);
export const margin_area = new Float32Array(4);
export const link_bounding = new Float32Array(4);
export const tempA = new Float32Array(2);
export const tempB = new Float32Array(2);

```
